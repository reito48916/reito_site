## Live Reloadingがなぜか聞かない場合の対処

pipで入ってるclickってライブラリが最新だとライブリローディングが効かない状態になるらしい。
```lang
$mkdocs serve
```
でローカルの確認環境を立ち上げたときのログでwatchうんぬん…と出ていない場合は効いてない。
```lang
$pip install "click==8.2.1"
```
でダウングレードしてやると、自分の場合は正常にライブリローディングが効くようになった。